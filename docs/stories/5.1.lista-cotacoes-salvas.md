# Story 5.1: Lista Cotações Salvas

**Epic**: EPIC-5 - Features de Alto Impacto (P1)
**RICE Score**: 33.3 (Reach: 5 | Impact: 10 | Confidence: 100% | Effort: 3 dias)
**Prioridade**: P1 (Alta)
**Estimativa**: 3 dias
**Status**: 🟡 Planejado

---

## 📋 User Story

```gherkin
As a: Vendedor STAGETEK
I want to: Ver lista completa de cotações criadas (filtrar por status, data, cliente)
So that: Eu encontre cotações antigas rapidamente e reaproveite propostas similares

Context: Atualmente o sistema gera PDF e envia email, mas não há página
         para listar todas as cotações. Vendedores perdem tempo procurando
         em pastas do email ou Drive.
```

---

## 🎯 Objetivos

### Objetivo Principal
Criar página `/cotacoes` com listagem completa, filtros avançados e ações (visualizar, reenviar, editar).

### Objetivos Secundários
- Histórico de cotações visível no detalhes da oportunidade
- Reutilizar componentes de filtro (FilterBar do Epic 2)
- Integração com Supabase Storage (download PDFs salvos)

---

## ✅ Acceptance Criteria

### AC-1: Página Listagem
```gherkin
Given: Estou logado como vendedor
When: Navego para /cotacoes
Then: Vejo lista de todas as cotações (paginação 20 por página)
And: Cada card mostra: número (QT-YYYYMM-NNN), cliente, valor total, status, data
And: Cards ordenados por created_at DESC (mais recentes primeiro)
```

### AC-2: Filtros Funcionais
```gherkin
Given: Estou na página /cotacoes
When: Aplico filtro "Status = Enviada"
Then: Vejo apenas cotações com status = 'sent'
And: URL atualiza: /cotacoes?status=sent
And: Filtro persiste ao recarregar página

When: Aplico filtro "Cliente = ABC Eventos"
Then: Vejo apenas cotações daquele cliente
And: Posso combinar múltiplos filtros (status + cliente + data)
```

### AC-3: Ações no Card
```gherkin
Given: Card de cotação "QT-202510-001"
When: Clico "Visualizar PDF"
Then: Download do PDF salvo no Storage (ou abre em nova aba)
And: Se PDF não existe, gera novo (fallback)

When: Clico "Reenviar Email"
Then: Abre modal com campo "Email destinatário"
And: Ao enviar, chama Edge Function com PDF anexado
And: Toast de sucesso: "Email enviado para fulano@email.com"

When: Clico "Editar" (apenas se status = 'draft')
Then: Navega para /oportunidades/:id/cotacao/:quotation_id/editar
And: Carrega items do JSONB (catalog + custom)
```

### AC-4: Link para Oportunidade
```gherkin
Given: Card de cotação vinculado à oportunidade "Pedido Set Luz"
When: Clico no nome da oportunidade (link)
Then: Navego para /oportunidades/:id/detalhes
And: Abro tab COTAÇÕES (ou HISTÓRICO se não existe tab)
```

### AC-5: Busca por Número
```gherkin
Given: Campo de busca "QT-YYYYMM-NNN"
When: Digito "202510"
Then: Filtro todas as cotações de Outubro/2025
And: Busca funciona com match parcial (QT-2025)
```

### AC-6: Estados Vazios
```gherkin
Given: Não existem cotações no sistema
When: Acesso /cotacoes
Then: Vejo mensagem: "Nenhuma cotação criada ainda"
And: Botão CTA: "Criar primeira cotação" → /oportunidades

Given: Filtros aplicados não retornam resultados
When: Status = 'sent' + Cliente = "XYZ"
Then: Vejo: "Nenhuma cotação encontrada com esses filtros"
And: Botão: "Limpar filtros"
```

---

## 🏗️ Arquitetura de Componentes

### Atomic Design (Protocol Notecraft™)

#### **Página** (Template)
```
src/pages/Cotacoes.tsx (30 linhas max)
├─ Imports: QuotationList organism, FilterBar organism
├─ Hook: useCotacoesPage (Zustand + Supabase)
└─ Layout: TopBar + FilterBar + QuotationList
```

#### **Organisms** (50 linhas max)
```
src/components/organisms/QuotationList.tsx (48 linhas)
├─ Map quotations → QuotationCard
├─ Loading state (skeleton cards)
├─ Empty state (nenhuma cotação)
└─ Paginação (shadcn Pagination component)

src/components/organisms/FilterBar.tsx (50 linhas - REUTILIZAR do Epic 2)
├─ Filtros: Status (select), Cliente (combobox), Data (date-range)
├─ Botão "Limpar filtros"
└─ Atualiza URL params + Zustand store
```

#### **Molecules** (35 linhas max)
```
src/components/molecules/QuotationCard.tsx (34 linhas)
├─ Props: quotation (tipo: Quotation)
├─ Layout: Card shadcn
│   ├─ Header: Número (QT-XXX) + Badge status
│   ├─ Body: Cliente, Valor (R$ 12.450,00), Data (há 2 dias)
│   └─ Footer: 3 botões (Visualizar, Reenviar, Editar)
└─ Hover effects: border-red-600

src/components/molecules/QuotationActions.tsx (28 linhas)
├─ 3 botões: Download, Reenviar, Editar
├─ Condicionais: Editar apenas se status = 'draft'
└─ Ações: handleDownloadPDF, handleResendEmail, handleEdit
```

#### **Atoms** (20 linhas max)
```
src/components/atoms/QuotationBadge.tsx (12 linhas)
├─ Props: status ('draft' | 'sent')
├─ Variants:
│   ├─ draft: bg-gray-100 text-gray-700
│   └─ sent: bg-green-100 text-green-700
└─ Render: <Badge variant={...}>Rascunho</Badge>
```

---

## 🗂️ Database & Schema

### Query Principal (Supabase)
```typescript
// src/hooks/useCotacoes.ts
const { data: quotations, error } = await supabase
  .from('quotations')
  .select(`
    *,
    opportunity:opportunities(id, title, client:clients(name))
  `)
  .eq('status', filters.status) // se filtro ativo
  .order('created_at', { ascending: false })
  .range(offset, offset + limit - 1); // paginação
```

### Índices Necessários (já existem do Sprint 0)
```sql
-- ✅ JÁ EXISTE (Sprint 0):
CREATE INDEX idx_quotations_status_created_at
  ON quotations(status, created_at DESC);

-- Performance: 8x faster queries para filtro status + sort por data
```

### Tipo TypeScript
```typescript
// src/types/quotation.ts
interface Quotation {
  id: string;
  quotation_number: string; // QT-YYYYMM-NNN
  opportunity_id: string;
  opportunity?: {
    id: string;
    title: string;
    client?: { name: string };
  };
  items: QuotationItem[]; // JSONB
  subtotal: number;
  freight: number;
  total: number;
  status: 'draft' | 'sent';
  sent_at?: string;
  sent_to_email?: string;
  pdf_url?: string; // Storage URL
  created_at: string;
  updated_at: string;
}
```

---

## 📋 Tasks Técnicos (Checklist)

### Dia 1: Estrutura Básica (8h)
- [ ] **Task 1.1**: Criar página `src/pages/Cotacoes.tsx` (template 30 linhas)
  - [ ] Importar TopBar, FilterBar, QuotationList
  - [ ] Setup layout: max-w-7xl mx-auto
  - [ ] Breadcrumb: Dashboard / Cotações
- [ ] **Task 1.2**: Criar hook `src/hooks/useCotacoes.ts` (60 linhas)
  - [ ] useQuery para fetch quotations (react-query)
  - [ ] Filtros em URL params (useSearchParams)
  - [ ] Paginação (offset/limit)
- [ ] **Task 1.3**: Criar organism `QuotationList.tsx` (48 linhas)
  - [ ] Map quotations → QuotationCard
  - [ ] Loading: 6 skeleton cards
  - [ ] Empty state: "Nenhuma cotação" + CTA
- [ ] **Task 1.4**: Criar molecule `QuotationCard.tsx` (34 linhas)
  - [ ] Layout: Card com header/body/footer
  - [ ] Display: número, cliente, valor, data, status
  - [ ] Props typing: QuotationCardProps
- [ ] **Task 1.5**: Criar atom `QuotationBadge.tsx` (12 linhas)
  - [ ] Variants: draft (gray), sent (green)

### Dia 2: Filtros e Ações (8h)
- [ ] **Task 2.1**: Reutilizar `FilterBar.tsx` do Epic 2 (ou criar se não existe)
  - [ ] Filtro Status: Select (Draft, Enviada, Todas)
  - [ ] Filtro Cliente: Combobox (busca)
  - [ ] Filtro Data: DateRangePicker (shadcn)
  - [ ] Botão "Limpar filtros"
- [ ] **Task 2.2**: Criar molecule `QuotationActions.tsx` (28 linhas)
  - [ ] Botão "Visualizar PDF" (Download icon)
  - [ ] Botão "Reenviar Email" (Mail icon)
  - [ ] Botão "Editar" (Edit2 icon, hidden se sent)
- [ ] **Task 2.3**: Implementar `handleDownloadPDF`
  - [ ] Fetch PDF do Storage (supabase.storage.from('pdfs').download())
  - [ ] Se não existe, gerar novo (fallback)
  - [ ] Trigger download com nome correto
- [ ] **Task 2.4**: Implementar `handleResendEmail`
  - [ ] Abrir modal com input email (EmailModal reutilizar)
  - [ ] Chamar Edge Function: send-quotation-email
  - [ ] Toast sucesso/erro
- [ ] **Task 2.5**: Implementar `handleEdit`
  - [ ] Verificar status = 'draft'
  - [ ] Navegar: /oportunidades/:id/cotacao/:quotation_id/editar
  - [ ] (Página de edição será Story 5.4 - fora deste escopo)

### Dia 3: Integração Storage e Refinamentos (8h)
- [ ] **Task 3.1**: Storage integration (download PDFs)
  - [ ] Criar função: downloadQuotationPDF(quotationId)
  - [ ] Buscar PDF do bucket 'pdfs'
  - [ ] Handle errors (PDF não encontrado → gerar novo)
- [ ] **Task 3.2**: Busca por número de cotação
  - [ ] Adicionar SearchInput no FilterBar
  - [ ] Query: .ilike('quotation_number', `%${search}%`)
- [ ] **Task 3.3**: Link para oportunidade
  - [ ] Tornar opportunity.title clicável
  - [ ] Navegar: /oportunidades/:id/detalhes
  - [ ] Abrir tab COTAÇÕES (ou HISTÓRICO)
- [ ] **Task 3.4**: Paginação funcional
  - [ ] Usar shadcn Pagination component
  - [ ] Controlar offset via URL: ?page=2
  - [ ] Mostrar "Mostrando 1-20 de 47 cotações"
- [ ] **Task 3.5**: Testes manuais e refinamentos
  - [ ] Testar filtros combinados (status + cliente)
  - [ ] Testar busca por número parcial (QT-2025)
  - [ ] Testar download PDF (caso existe e não existe)
  - [ ] Testar reenviar email (novo destinatário)
  - [ ] Mobile: cards responsivos, filtros em modal

---

## 🎨 UI/UX Specifications

### Layout Desktop (1024px+)
```
┌─────────────────────────────────────────────┐
│ TopBar (Dashboard | Clientes | Oportunidades│ Cotações) │
├─────────────────────────────────────────────┤
│ Breadcrumb: Dashboard / Cotações            │
├─────────────────────────────────────────────┤
│ FilterBar                                    │
│ [Status ▼] [Cliente 🔍] [Data 📅] [Limpar]  │
├─────────────────────────────────────────────┤
│ QuotationList (Grid 3 cols)                 │
│ ┌─────────┐ ┌─────────┐ ┌─────────┐        │
│ │ QT-001  │ │ QT-002  │ │ QT-003  │        │
│ │ ABC     │ │ XYZ     │ │ DEF     │        │
│ │ R$ 12k  │ │ R$ 8k   │ │ R$ 15k  │        │
│ │ [👁][✉][✏]│ │ [👁][✉] │ │ [👁][✉][✏]│        │
│ └─────────┘ └─────────┘ └─────────┘        │
│                                              │
│ Paginação: « 1 2 3 ... 12 »                │
└─────────────────────────────────────────────┘
```

### Layout Mobile (<768px)
```
┌─────────────────────┐
│ ☰ Cotações          │
├─────────────────────┤
│ [🔍 Buscar QT-XXX]  │
│ [🎛 Filtros (3)]     │ ← Badge com count de filtros ativos
├─────────────────────┤
│ ┌─────────────────┐ │
│ │ QT-202510-001   │ │
│ │ ABC Eventos     │ │
│ │ R$ 12.450,00    │ │
│ │ Há 2 dias       │ │
│ │ [👁] [✉] [✏]    │ │
│ └─────────────────┘ │
│ ┌─────────────────┐ │
│ │ QT-202510-002   │ │
│ │ ...             │ │
└─────────────────────┘
```

### QuotationCard Design
```tsx
// Visual spec
<Card className="group hover:border-red-600 transition-all">
  {/* Header */}
  <CardHeader className="flex-row justify-between items-start">
    <div>
      <h3 className="font-semibold">QT-202510-001</h3>
      <Link to={`/oportunidades/${id}`} className="text-sm text-muted-foreground hover:text-red-600">
        Pedido Set Luz
      </Link>
    </div>
    <QuotationBadge status="sent" />
  </CardHeader>

  {/* Body */}
  <CardContent className="space-y-2">
    <p className="text-sm">
      <Building2 className="inline mr-1 h-4 w-4" />
      ABC Eventos Ltda
    </p>
    <p className="text-2xl font-bold text-red-600">
      R$ 12.450,00
    </p>
    <p className="text-xs text-muted-foreground">
      Criada há 2 dias • Enviada para contato@abc.com
    </p>
  </CardContent>

  {/* Footer */}
  <CardFooter className="gap-2">
    <Button variant="outline" size="sm" onClick={handleDownloadPDF}>
      <Eye className="h-4 w-4" />
      Visualizar
    </Button>
    <Button variant="outline" size="sm" onClick={handleResendEmail}>
      <Mail className="h-4 w-4" />
      Reenviar
    </Button>
    {status === 'draft' && (
      <Button variant="outline" size="sm" onClick={handleEdit}>
        <Edit2 className="h-4 w-4" />
        Editar
      </Button>
    )}
  </CardFooter>
</Card>
```

### Cores e Tokens
- **Primary**: red-600 (#dc2626) - STAGETEK brand
- **Status badges**:
  - Draft: gray-100 bg, gray-700 text
  - Sent: green-100 bg, green-700 text
- **Hover**: border-red-600 (cards)
- **Valores R$**: text-red-600 font-bold

---

## 🧪 Testing Strategy

### Testes Manuais (Checklist QA)
```gherkin
# Happy Path
[ ] Listar 20 cotações com dados corretos (número, cliente, valor)
[ ] Filtrar por status "Enviada" → retorna apenas 'sent'
[ ] Filtrar por cliente "ABC" → retorna apenas aquele cliente
[ ] Baixar PDF existente → download funciona
[ ] Reenviar email → modal abre, email enviado, toast sucesso
[ ] Editar cotação draft → navega para /editar
[ ] Click em oportunidade → navega para detalhes
[ ] Paginação → página 2 carrega próximos 20

# Edge Cases
[ ] Nenhuma cotação → empty state com CTA
[ ] PDF não existe → gerar novo (fallback)
[ ] Reenviar com email inválido → erro de validação
[ ] Editar cotação 'sent' → botão hidden
[ ] Busca "QT-999999" → "Nenhuma cotação encontrada"
[ ] Mobile: filtros em modal, cards stacked
```

### Testes Automatizados (Futuro - Sprint 2)
```typescript
// tests/cotacoes.spec.ts (Playwright)
test('should list quotations', async ({ page }) => {
  await page.goto('/cotacoes');
  await expect(page.locator('h3:has-text("QT-")')).toHaveCount(20);
});

test('should filter by status', async ({ page }) => {
  await page.goto('/cotacoes');
  await page.selectOption('[name="status"]', 'sent');
  await expect(page).toHaveURL(/status=sent/);
  // Verify all cards have "Enviada" badge
});
```

---

## 📊 Métricas de Sucesso

### Métricas de Produto
- **Adoção**: 80% das cotações acessadas via lista (vs 0% hoje)
- **Reuso**: 30% das novas cotações reutilizam itens de antigas
- **Tempo de busca**: <5s para encontrar cotação (vs 2min em email/Drive)

### Métricas de Performance
- **Page Load**: <500ms (inicial, sem dados)
- **Query Time**: <100ms (20 cotações + joins)
- **Download PDF**: <1s (Storage fetch)

### Métricas de Qualidade
- **Protocol Notecraft™**: 100% (atoms ≤20, molecules ≤35, organisms ≤50)
- **TypeScript**: Zero `any`
- **Mobile**: Lighthouse Score >85
- **Acessibilidade**: WCAG AA (aria-labels, keyboard nav)

---

## 🔗 Dependências

### Depende de (Bloqueadores)
- ✅ Sprint 0: Database + RLS completo (COMPLETO - 29 Out)
- ✅ Cotação MVP: quotations table + PDFs (COMPLETO - Sprint 1 Day 1)
- ✅ Supabase Storage: bucket 'pdfs' (EXISTE)
- ✅ Edge Function: send-quotation-email (DEPLOYED)

### Habilita (Próximas Stories)
- Story 5.4: Editar Cotação (página /editar)
- Story X: Tab Cotações em Detalhes Oportunidade
- Epic 3: Relatórios (análise de cotações enviadas vs convertidas)

---

## ⚠️ Riscos e Mitigações

### Risco 1: Storage Limits (2GB free)
**Impacto**: Alto (bloquearia novos PDFs)
**Mitigação**:
- Monitorar usage: criar alerta 80%
- Compressão de PDFs (50% redução)
- Cold storage para PDFs >90 dias

### Risco 2: Download PDF falhando
**Impacto**: Médio (usuário não consegue visualizar)
**Mitigação**:
- Fallback: gerar novo PDF se não existe
- Retry logic (3 tentativas)
- Mensagem clara: "Regenerando PDF..."

### Risco 3: Filtros complexos (performance)
**Impacto**: Baixo (query lenta)
**Mitigação**:
- Índices já existem (Sprint 0)
- Limitar a 1000 resultados totais
- Paginação server-side

---

## 📚 Referências

### Documentação
- `docs/prd/epic-5-features-alto-impacto.md` - Epic parent
- `docs/architecture/database-schema.md` - Schema quotations
- `SECURITY-AUDIT.md` - Índices de performance

### Benchmarks UX
- RD Station CRM: Lista de propostas
- PipeDrive: Produtos e cotações
- HubSpot: Quotes module

### Componentes shadcn
- Card: https://ui.shadcn.com/docs/components/card
- Pagination: https://ui.shadcn.com/docs/components/pagination
- Combobox: https://ui.shadcn.com/docs/components/combobox

---

**Última atualização**: 29 de Outubro de 2025 - Sprint 1 planejamento
